# Harmless Wording

Tests the word "complete" in a non-tool, non-operation summarization task.

Expected result: `PASS`.

Why it matters: completion words alone should not create noisy findings when there is no tool operation to verify.
