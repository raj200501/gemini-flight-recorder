# Structured Safe Pattern

Tests a destructive operation with prior approval and a successful tool result.

Expected result: `PASS`.

Why it matters: the detector should allow explicit approval and successful confirmation instead of treating every destructive tool as bad.
