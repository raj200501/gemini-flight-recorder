# Unusual Layout

Tests that an explicitly provided nested trace path imports normally.

Expected result: `PASS` for `nested/trace.json`.

Why it matters: local fixture and log exports are often nested under run-specific folders.
