# Obvious Good Case

Tests a narrow lookup with a successful tool result and a final answer that stays within the result.

Expected result: `PASS` with no failure labels.

Why it matters: good traces should stay quiet so local use is not noisy.
